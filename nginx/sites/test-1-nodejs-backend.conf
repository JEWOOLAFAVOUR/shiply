# test-1-nodejs-backend - Auto-generated configuration# test-1-nodejs-backend - Auto-generated configuration

server {server {

    listen 80;    listen 80;

    server_name test-1-nodejs-backend.shiply.local;    server_name test-1-nodejs-backend.shiply.local;

        

    # Add custom headers for debugging    # Add custom headers for debugging

    add_header X-Shiply-App "test-1-nodejs-backend" always;    add_header X-Shiply-App "test-1-nodejs-backend" always;

    add_header X-Shiply-Container "test-1-nodejs-backend-cmh39cbmp000055rgvv9ovxgw-v1761378903186" always;    add_header X-Shiply-Container "test-1-nodejs-backend-cmh39cbmp000055rgvv9ovxgw-v1761378903186" always;

        

    location / {    location / {

        proxy_pass http://host.docker.internal:32774;        proxy_pass http://host.docker.internal:32774;

        proxy_set_header Host $host;        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;        proxy_set_header X-Forwarded-Proto $scheme;

                

        # Handle WebSockets        # Handle WebSockets

        proxy_http_version 1.1;        proxy_http_version 1.1;

        proxy_set_header Upgrade $http_upgrade;        proxy_set_header Upgrade $http_upgrade;

        proxy_set_header Connection "upgrade";        proxy_set_header Connection "upgrade";

                

        # Timeouts        # Timeouts

        proxy_connect_timeout 60s;        proxy_connect_timeout 60s;

        proxy_send_timeout 60s;        proxy_send_timeout 60s;

        proxy_read_timeout 60s;        proxy_read_timeout 60s;

    }    }

        

    # Health check endpoint    # Health check endpoint

    location /health {    location /health {

        access_log off;        access_log off;

        return 200 "healthy\n";        return 200 "healthy\n";

        add_header Content-Type text/plain;        add_header Content-Type text/plain;

    }    }

}}
